# تجهيز النظام للرفع على Render

تم تجهيز هذا المشروع ليعمل مباشرة على **Render** باستخدام **Flask + Gunicorn + SQLite**.

## ما الذي تم تجهيزه
- إضافة ملف `render.yaml` للرفع التلقائي على Render.
- إضافة `wsgi.py` لتشغيل التطبيق عبر Gunicorn.
- تهيئة مسارات قابلة للكتابة للبيانات والملفات المرفوعة.
- نسخ قاعدة البيانات الافتراضية تلقائياً إلى القرص الدائم أول تشغيل.
- إضافة مسار فحص صحي: `/health`.
- تحديث `requirements.txt` ليحتوي على الحزم المطلوبة فعلياً للتشغيل على Render.

## خطوات الرفع
1. فك الضغط عن المشروع.
2. ارفع محتويات مجلد المشروع إلى GitHub.
3. من Render اختر **New +** ثم **Blueprint**.
4. اربط المستودع، وسيقرأ Render ملف `render.yaml` تلقائياً.
5. أكمل الإنشاء وانتظر أول نشر.

## ملاحظات مهمة
- قاعدة البيانات ستُحفظ داخل القرص الدائم في المسار `/var/data/schema.db`.
- الملفات المرفوعة من داخل النظام ستُحفظ في `/var/data/uploads`.
- أول تشغيل ينسخ قاعدة البيانات المرفقة تلقائياً إذا لم تكن موجودة بعد.
- إذا أردت تغيير اسم الخدمة على Render عدّل قيمة `name` داخل `render.yaml`.

## التشغيل المحلي
```bash
pip install -r requirements.txt
python run.py
```

## الدخول الافتراضي
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

- اسم المستخدم: `supervisor`
- كلمة المرور: `123456`
