-- ============================================================
--  ملف واحد لتجهيز النظام v2 (ينشئ DB + v1 + v2 + البيانات التجريبية)
--  استورده من phpMyAdmin بنقرة واحدة أو من سطر الأوامر:
--    mariadb -uroot < database/setup_all.sql
-- ============================================================
SET NAMES utf8mb4;
SOURCE schema.sql;
SOURCE migration_v2.sql;
